#include<bits/stdc++.h>
using namespace std;
void party(string a, string b, string c, string s){
    sort(s.begin(), s.end());
    sort(c.begin(), c.end());
    if(s==c) cout<<"YES";
    else cout<<"NO";
}
int main(){
    string a,b,c,s;
    cin>>a>>b>>c>>s;
    s=a+b;
    party(a,b,c,s);
}
